# Release Mapping

> Maps medium releases to upstream tags and commits.

This file is machine-managed by the release script. Avoid manual edits so the
release history stays consistent with the generated provenance mapping.
